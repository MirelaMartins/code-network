import Technology from '@/enums/Technology';
import { LeanDocument } from 'mongoose';

// const getAllTechnologies = async (): Promise<LeanDocument<Technology>[]> => Technology[enum];

// export {  getAllTechnologies };
